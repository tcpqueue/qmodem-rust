# 服务管理与模组业务共用此配置。首次部署后按设备实际情况修改。
version = 1

[server]
listen = "127.0.0.1"
port = 8088
# Linux 网络设备，如 br-lan、eth0；空字符串表示不限制网卡。
interface = ""

[logging]
# off / error / warn / info / debug / trace
level = "info"
# text / json；输出到 stderr，由 procd 收集至系统日志。
format = "text"

[auth]
# qmodemd init-auth 生成访问令牌并仅保存 SHA-256。非回环监听必须配置认证。
token_hash = ""

[storage]
sqlite = "/etc/qmodem-rust/data.sqlite3"
# MT5700 软件卡槽记录：服务重启保留、路由器重启清除。目录由服务创建为 0700。
runtime_dir = "/tmp/qmodem-rust"

# 模组业务尚在迁移中；以下字段先固定配置契约。
# [[modems]]
# id = "modem_1"
# name = "主模组"
# enabled = true
# manufacturer = "quectel"
# model = "rm520n-gl"
# platform = "qualcomm"
# at_port = "/dev/ttyUSB2"
# sms_at_port = "/dev/ttyUSB3"
# interface = "wwan0"
# bus = "usb"
# pdp_index = 1
# apn = ""

# TD Tech MT5700 使用 manufacturer="tdtech"、model="mt5700m-cn"、platform="hisilicon"。

# 可选频段资料覆盖（不限制 AT 设置命令；空数组表示该类别无可选项）。
# [modems.bands]
# umts = [1, 5, 8]
# lte = [1, 3, 5, 8, 28, 38, 40, 41, 71]
# nr_nsa = [41, 78, 79]
# nr = [41, 78, 79]
